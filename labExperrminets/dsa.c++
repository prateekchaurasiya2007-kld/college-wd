#include<iostream>
using namespace std;
int main()
{
    int num,sum=o;
    cout<<"Enter a 4 digit number">>
    cin>>a;
    while(a!=0){ 
   sum=sum+a%10;
   a=a/10;
 }
  cout<<"sum of digit">>sum
  return 0;
}